{
  "name": "db-plus-attractor",
  "version": "1.0.0",
  "description": "Production-ready SaaS backend for CRM and lead capture.",
  "main": "src/server.js",
  "type": "commonjs",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "dev": "nodemon src/server.js",
    "start": "node src/server.js",
    "lint": "node --check src/server.js && node --check src/app.js && find src -name '*.js' -print0 | xargs -0 -n1 node --check",
    "test": "node --test"
  },
  "keywords": [
    "crm",
    "leads",
    "express",
    "mongodb",
    "mongoose",
    "saas"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "compression": "^1.8.1",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "helmet": "^8.1.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.19.1",
    "morgan": "^1.10.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
